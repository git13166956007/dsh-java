spring:
  application:
    name: dsh-java

server:
  port: ${SERVER_PORT:8080}

dsh:
  model:
    base-url: ${DEEPSEEK_BASE_URL:https://api.deepseek.com}
    api-key: ${DEEPSEEK_API_KEY:}
    name: ${DEEPSEEK_MODEL:deepseek-flash}
